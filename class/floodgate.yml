parameters:
  kapitan:
    compile:
      - input_paths:
          - floodgate/component/app.jsonnet
        input_type: jsonnet
        output_path: apps/
      - input_paths:
          - floodgate/component/main.jsonnet
        input_type: jsonnet
        output_path: floodgate/
